重力引擎驱动器 —— 使用说明
================================

## 快速开始

1. 在飞船上放置一个可编程方块（PB）。
2. 放置若干重力发生器和至少一个驾驶舱。
3. （可选）将重力发生器、驾驶舱、LCD 面板加入同一个方块编组，并在 CustomData 中填写编组名。
4. 将脚本粘贴到 PB 中，点击"运行"。
5. 坐上驾驶舱，按 WASD 即可飞行。惯性抑制器与飞船同步。

## 终端指令

在 PB 终端中运行以下指令（区分大小写）：

| 指令            | 效果               |
|----------------|-------------------|
| `SwitchGrav`   | 切换引擎开启 / 关闭 |
| `ChangeFacing` | 在默认朝向和备选朝向之间切换 |
| `SetFacing 朝向`| 手动指定朝向（如 `SetFacing Up`） |
| `UpdateBlocks` | 立即刷新方块列表 |

## 参数配置（CustomData）

在 PB 的 CustomData 中修改以下参数，修改后重新编译即可生效：

| 参数名          | 默认值    | 说明 |
|----------------|----------|------|
| 自定义编组名称   | （空）    | 方块编组名。留空 = 全舰搜索；填写后仅在指定编组内查找方块 |
| 最大出力加速度   | 9.81     | 单台重力发生器的最大加速度（m/s²） |
| 停止阈值        | 0.01     | 速度低于此值（m/s）时完全停止出力 |
| 低速区间阈值     | 9.8      | 低于此速度（m/s）进入柔和刹车模式 |
| 比例常数K       | 1        | 柔和刹车力度，调高可能导致震荡 |
| 默认朝向        | Front    | 飞船的前方是驾驶舱的哪个方向 |
| 备选朝向        | Left     | ChangeFacing 切换到的方向 |
| 更新间隔        | 600      | 方块列表自动刷新间隔（ticks，600 = 每 10 秒） |
| 每帧最大写入数   | 10       | 每帧最多操作几台发生器，大船可调高 |
| 显示刷新间隔     | 5        | 屏幕信息刷新频率（ticks，5 ≈ 12 Hz） |
| 滚动窗口大小     | 60       | 平均耗时统计的窗口大小（帧数） |

## 可选：编组模式

将方块加入编组可以实现"只控制特定方块"：

1. 在终端中选中所有要用到的重力发生器、驾驶舱、LCD 面板。
2. 给它们起一个编组名（如 "引擎"）。
3. 在 PB 的 CustomData 中设置 `自定义编组名称 = 引擎`。
4. 重新编译脚本。

如果 LCD 面板在同一编组内，性能信息会自动显示到所有面板上。

## 朝向说明

`默认朝向` 和 `备选朝向` 的可选值：
`Front`（前）、`Back`（后）、`Left`（左）、`Right`（右）、`Up`（上）、`Down`（下）

朝向表示"飞船的前方是驾驶舱的哪个方向"。如果你的驾驶舱不是朝前安装的（比如朝上或朝左），需要修改此参数。
